# 部署说明

## 架构

本项目通过 GitHub + Vercel 持续部署，Vercel 不执行构建，直接以 `dist/` 目录作为静态输出。

```
vercel.json
  installCommand: echo skip
  buildCommand:   echo skip
  outputDirectory: dist
```

## 更新流程

每次修改源码后，需手动构建并提交 dist，才能触发 Vercel 更新。

```bash
# 1. 构建（跳过 TS 类型检查）
npx vite build

# 2. 提交源码 + dist
git add src/pages/xxx.vue dist/
git commit -m "feat: ..."

# 3. 推送，Vercel 自动拉取 dist 部署
git push origin main
```

> 不要用 `npm run build`，该命令会先运行 `vue-tsc` 类型检查，当前存在既有 TS 报错会导致中断。

## 线上地址

见 Vercel 项目面板，部署成功后约 30 秒生效。
